1. Fix api list entry display is not showing when running react

Note: able to make request with filter with "http://127.0.0.1:8000/api/lists/?title=title2"
    Need to find out how to filter by null for parent_list. Also see if we can have multiple filters on single view